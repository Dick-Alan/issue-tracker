<div class='articles'>
    <h1>Issue Tracker.</h1>
    <div class='return-button'>
        <%= link_to "[+] Add a new issue", new_article_path %>
    </div>
    There are <%= Article.public_count %> unresolved issues. 
    <ul>
    <li class='article-summary'>
    <div>Title:</div>
    <div>Author:</div>
    <div>Time:</div>
    <div>Status:</div>
    <div>Priorty:</div>

    </li>
    <br>
    <% @articles.filter { |e| e.status== 'unresolved'}.each do |article|%>
    <%= render "article", article: article %>
    <% end %>
    </ul>
     There are <%= Article.resolved_count %> resolved issues.

    <% @articles.filter { |e| e.status== 'resolved'}.each do |article|%>
    <%= render "article", article: article %>
    <% end %>
    </ul>
   
<div>